[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    integration: Tests d'intégration nécessitant des données réelles
    slow: Tests lents à exécuter